---
description: 组件与模块导入必须使用 @ 别名
alwaysApply: true
---

# 导入规范：@ 别名

组件及模块导入**必须**使用 `@` 路径别名，禁止使用相对路径。

## 规则

- 使用 `@/components/...`、`@/app/...` 等别名导入
- 禁止使用 `./`、`../` 相对路径导入项目内模块

## 示例

```typescript
// ✅ 正确
import { OpenClawApp } from '@/components/open-claw-app';
import type { TabKey } from '@/components/types';
import { useGateway } from '@/components/hooks/use-gateway';

// ❌ 错误
import { OpenClawApp } from './open-claw-app';
import type { TabKey } from '../types';
```
